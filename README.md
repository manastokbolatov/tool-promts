# Tool Prompts

Structured output is important because JSON has a fixed format that a program can parse and use to call tools automatically.

My code is responsible for validating the JSON, calling the selected tool, handling errors, and sending the tool result back to the model.